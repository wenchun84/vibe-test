# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Overview

This is `vibe-test`, currently a bare repository containing only a README. There is no application code, build system, test framework, or tooling configuration yet.

## Current State

- `README.md` — project title only
- No package manifest (no `package.json`, `pyproject.toml`, `Cargo.toml`, etc.)
- No build, lint, or test commands exist yet

## Guidance for Future Work

When code is added to this repository, update this file with:

1. **Commands** — how to install dependencies, build, lint, run tests (including a single test)
2. **Architecture** — the big-picture structure once there are multiple modules that need to be understood together

Until then, there are no project-specific conventions to follow. If you introduce a language or framework, establish its standard tooling (formatter, linter, test runner) and document the commands here.
